import { SortByWordsPipe } from './sort-by-words.pipe';

describe('SortByWordsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
