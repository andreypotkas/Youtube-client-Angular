import { AgeStatusDirective } from './age-status.directive';

describe('AgeStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new AgeStatusDirective();
    expect(directive).toBeTruthy();
  });
});
